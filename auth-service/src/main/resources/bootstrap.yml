server:
  port: ${PORT:8001}

spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI:http://127.0.0.1:8888}
      username: config_user
      password: config_password
      name: ureka, mysql-system